class CardGame:


  def check_for_ace(self, card):
    #   v should be == instead of = for conditional checking
    if card.value = 1:
      return True
    # no colon following else:
    else
      return False

# def is misspelt, there is no comma between card1 and card2
  dif highest_card(self, card1 card2):
    # indentation is wrong, should be one step further indented
  if card1.value > card2.value:
    #   card is not declared as an argument, only card1 and card2 are declared albeit improperly
    return card
  else:
    return card2



def cards_total(self, cards):
    # total variable is not declared properly, needs to be assigned value
  total
  for card in cards:
    total += card.value
    return "You have a total of" + total